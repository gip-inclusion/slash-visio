SLACK_SIGNING_SECRET=
SLACK_BOT_TOKEN=
# For scripts/visio-count-24h.sh — create via:
#   scw cockpit token create name=visio-stats token-scopes.0=read_only_logs
SCW_COCKPIT_LOG_TOKEN=
# For ad-hoc memory/CPU metrics probes — create via:
#   scw cockpit token create name=visio-metrics token-scopes.0=read_only_metrics
SCW_COCKPIT_METRICS_TOKEN=
